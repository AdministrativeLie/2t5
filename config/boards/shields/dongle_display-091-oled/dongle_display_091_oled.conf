#define LV_HOR_RES_MAX  128  // Keep horizontal resolution
#define LV_VER_RES_MAX  32   // Change from 64 to 32

# Display settings
CONFIG_ZMK_DISPLAY=y

CONFIG_BT_BAS=n
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y

# 48 hour timeout
CONFIG_ZMK_IDLE_TIMEOUT=172800000

